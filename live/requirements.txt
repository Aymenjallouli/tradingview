# Real-time paper trading engine — dependencies (Python 3.10+)
pandas>=2.0.0            # candle dataframes + indicator math
requests>=2.31.0        # REST history warmup from Binance
websocket-client>=1.7.0 # live event-driven kline stream (no polling)
yfinance>=1.5.0         # forex candles (no free forex tick feed exists)
fastapi>=0.110.0        # dashboard web server
uvicorn[standard]>=0.29.0  # ASGI server that runs FastAPI
python-dotenv>=1.0.0    # loads the .env file (Claude token + settings)

# NOTE: the Claude "helper" (live commentary) shells out to the `claude` CLI
# from your Claude Code subscription — that is NOT a pip package. If `claude`
# isn't installed (e.g. on a plain VPS), the helper just shows "offline" and
# everything else keeps working. Install Claude Code + sign in to enable it.
